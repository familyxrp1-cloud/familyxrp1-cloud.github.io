{
  "name": "family-meme-api",
  "version": "1.0.0",
  "description": "Server-side relay for the $FAMILY Meme Lab — holds the OpenAI key so visitors never need their own",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
