# Required — billed to YOUR OpenAI account per image generated (gpt-image-1 is roughly a few cents each)
OPENAI_API_KEY=sk-your-key-here

PORT=3100

# Lock this down to your real domain once it's live — "*" is only fine while testing
CORS_ORIGIN=https://familyxrp.com

# Abuse protection — tune to match your budget
PER_IP_HOURLY_LIMIT=5
DAILY_GLOBAL_LIMIT=150
