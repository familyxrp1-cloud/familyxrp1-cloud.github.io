# Optional: Helius key for higher rate limits (helius.dev — free tier)
# Without it, uses Ankr free public Solana RPC automatically.
# HELIUS_KEY=your_key_here

PORT=3000
CORS_ORIGIN=*
HELIUS_DAILY_BUDGET=28000
